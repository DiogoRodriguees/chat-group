# chat-group

### Execute server

Use [bundler](https://bundler.io/) to install depenncies.

```baash
  # entry in server folder
  $ cd server

  # install dependencies
  $ bundle install

  # run server
  $ ruby server.rb
```
